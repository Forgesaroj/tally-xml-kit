# Contributing

Open an issue describing the Tally workflow and include a redacted request/response sample. New builders must escape scalar values, document raw-XML boundaries, and include tests for valid and malformed inputs. Never commit customer names, PANs, vouchers, credentials, or live company data.
